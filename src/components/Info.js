import React from "react";
import "./styles/Info.css";

const Info = () => {
  return (
    <section className="info">
      <h2><em>What we do?</em></h2>
      <p>
        We use a fast responsive algorithm and machine learning to detect
        real-time pollution from social platforms and update directly to our
        dashboard.
      </p>
    </section>
  );
};

export default Info;
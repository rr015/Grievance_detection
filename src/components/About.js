import React from "react";
import "./styles/About.css";

const About = () => {
  return (
    <section className="about">
      <h2>About</h2>
      <p>
        Pollution Detection is a web application that uses detection-based
        algorithms, APIs, and bots to report real-time pollutions from different
        sources across social media platforms. Our goal is to provide data that
        is usually inaccessible in real time.
      </p>
    </section>
  );
};

export default About;